[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "gettowork"
dynamic = ["version"]  # read from src/gettowork/__init__.py: the one place to bump it
description = "Get To Work: an edutainment text adventure that teaches local open-weight LLMs and the Jev typed-judgment API."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{ name = "markelphoenix" }]
keywords = ["llm", "local-llm", "education", "game", "jev", "gguf", "ollama"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Topic :: Games/Entertainment",
  "Topic :: Education",
]
dependencies = [
  "rich>=13.0",
  "psutil>=5.9",
  "huggingface_hub>=1.1",  # 1.1 added hf_hub_download(tqdm_class=...), which drives the download bar
]

[project.optional-dependencies]
llamacpp = ["llama-cpp-python>=0.3.0"]
dev = ["pytest>=8.0"]

[project.scripts]
gettowork = "gettowork.cli:main"  # the terminal version

[project.gui-scripts]
gettowork-gui = "gettowork.launcher:gui_main"  # the game's own window (what Steam launches)

[tool.setuptools.dynamic]
version = { attr = "gettowork.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
gettowork = ["assets/*"]  # the window icon

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
